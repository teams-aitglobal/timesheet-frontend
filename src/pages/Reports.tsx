import { FileBarChart } from "lucide-react";

import { ComingSoon } from "@/components/layout/ComingSoon";

export default function Reports() {
  return (
    <ComingSoon
      title="Reports"
      description="Reports will appear here once the API is connected."
      icon={FileBarChart}
    />
  );
}
